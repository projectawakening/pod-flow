{
  "name": "@pod-flow/location-bounded-mock",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "description": "Package for generating mock PODs, and a GPC proof of distance assertions for location bounding.",
  "scripts": {
    "generate-mock-pods": "ts-node scripts/generateMockLocationPods.ts",
    "generate-distance-proof": "ts-node scripts/generateDistanceProof.ts",
    "run-location-bounded-mock": "pnpm run generate-mock-pods && pnpm run generate-distance-proof ./proof-configs/distanceProofConfig.ts ./pod-data/location_pods.json"
  },
  "dependencies": {
    "@pcd/gpc": "^0.4.0",
    "@pcd/pod": "^0.5.0",
    "@pcd/proto-pod-gpc-artifacts": "^0.13.0"
  }
}
